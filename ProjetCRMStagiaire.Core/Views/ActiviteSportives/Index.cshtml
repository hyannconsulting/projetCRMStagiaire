@model IEnumerable<ProjetCRMStagiaire.Core.Data.ActiviteSportive>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreDePlace)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lieu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreDePlace)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lieu)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ActiviteSportiveId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ActiviteSportiveId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ActiviteSportiveId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
